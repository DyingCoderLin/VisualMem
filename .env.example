# VisualMem Configuration

# ============================================
# Module Selection
# ============================================

# Capture module: 'screenshot' (more options in future)
CAPTURER_TYPE=screenshot

# Preprocessor: 'simple' (frame difference) or 'vllm' (intelligent filter)
PREPROCESSOR_TYPE=simple

# VLM Backend: 'api' (cloud API) or 'local' (on-device model)
VLM_BACKEND_TYPE=api

# Storage: 'lancedb' (vector database)
STORAGE_TYPE=lancedb

# ============================================
# Preprocessing Parameters
# ============================================

# Frame difference threshold for SimpleFilter (0.0-1.0)
# Lower = more sensitive (captures smaller changes)
# Higher = less sensitive (only captures major changes)
# Default: 0.006 (based on ScreenPipe)
SIMPLE_FILTER_DIFF_THRESHOLD=0.006

# ============================================
# VLM API Configuration (for VLM_BACKEND_TYPE=api)
# ============================================

# Your API key (OpenAI, Anthropic, etc.)
VLM_API_KEY=your_api_key_here

# API endpoint (OpenAI-compatible format)
VLM_API_URI=https://api.openai.com/v1/chat/completions

# Model name
VLM_API_MODEL=gpt-4o

# ============================================
# Local VLM Configuration (for VLM_BACKEND_TYPE=local)
# ============================================

# Path to your local VLM model
LOCAL_VLM_MODEL_PATH=/path/to/your/local/model

# ============================================
# Storage Configuration
# ============================================

# Database path
DB_PATH=./visualmem_db

# ============================================
# Runtime Parameters
# ============================================

# Screenshot capture interval in seconds
# Lower = more frequent captures, higher CPU/API usage
# Higher = less frequent, may miss quick changes
CAPTURE_INTERVAL_SECONDS=3
